using System;
using System.Collections.Generic;
using System.Linq;

namespace Testing_one
{
    class Program
    {
        static void Main(string[] args)
        {
            //var numbers = new Dictionary<double, int>
            //{
            //    { 18.99, 4},
            //    { 88.3, 2 },
            //    { 5.63, 7 },
            //    { 44.4, 4 }
            //};

            //var result = numbers.OrderBy(x => x.Value).ThenBy(x => x.Key);
            //var selectFuncton = result.Select(x => "key is " + x.Key + " and the value is " + x.Value);
            //Console.WriteLine(string.Join(" ,", selectFuncton));

            //var arr = new int[] { 10, 10, 20, 40, 40, 20, 50, 10, 15 };

            //var dict = arr.Distinct().ToDictionary(x => x, x => 0);
            //foreach (var num in arr)
            //{
            //    dict[num]++;
            //}
            //Console.WriteLine(string.Join("   ", dict.Select(x => x.Key + " => " + x.Value)));

            //var list = Console.ReadLine().Split(' ').ToList();

            //var sortedList = list.OrderByDescending(x => x).Take(3);
            //Console.WriteLine(string.Join(" ", sortedList));
        }
    }
}
